# 验证记录

[English](TEST_REPORT.en.md) · [返回说明](README.md)

验证日期：2026-09-22。环境：本机 macOS、Node.js 24.14.1、Chromium。所有学生、姓名和作品均为隔离测试数据；未读取或迁移原项目的课堂数据库。

## 自动化检查

`npm run check` 通过：14 项测试通过，0 项失败，生产构建成功。`npm audit --omit=dev` 报告 0 项已知漏洞。

覆盖教师权限、同源写入、课堂码加入、两次提交顺序、独立开关、学校名单校验、必填和长度、并发去重、导出 CSV、会话注销、服务器重启、开始新课后的隔离，以及真实 HTTP SSE 通知。

并发用例在单机 Fastify 注入层模拟 150 个共享出口地址的参与端，共完成 300 次有效提交；所有记录均可通过分页完整读取。此用例不代表经过目标校网验证的容量上限。

## 学校名单更新

2026-09-22，按用户提供的原名和顺序，将占位名单替换为 6 所学校。重新执行 `npm run check`，14 项测试及生产构建均通过。另在隔离数据库中逐项校验：加入接口返回完整的 6 项名单，6 所学校均可提交，“待补充学校”被服务端拒绝。接口测试读取配置中的学校，浏览器脚本选择实际下拉选项，不再依赖占位名称。

本次名单变更没有重新执行完整浏览器闭环；下一节保留首版的浏览器验收记录。

## 浏览器闭环

使用一个教师页面和两个独立学生浏览器上下文，完成：

1. 两个环节关闭时可加入、不能提交；老师开放后学生自动恢复填写。
2. 学生第一次填写学校、姓名、领域、应用场景和价值；刷新可恢复未提交草稿。
3. 第一次提交后，另一名学生和教师自动看到对应的列表行，未手动刷新。
4. 老师关闭发现环节，未提交的学生表单被禁用；开放设计后，未完成发现的学生仍不能越过第一步。
5. 完成第二次提交，沿用姓名和学校，同学可切换到设计列表查看。
6. 两名学生完成两次提交后，关闭两个环节，列表仍保留；刷新后提交记录恢复。
7. 姓名搜索正确过滤列表；桌面宽度 1366 像素、手机宽度 390 像素下完成检查。手机整页没有横向溢出，宽表格可在内部左右滚动。

最终闭环运行未出现浏览器脚本异常或控制台错误。已目视检查教师桌面、学生桌面、学生手机截图，文件位于本地 `output/playwright/`，不进入 Git。早期未登录状态的预期 401 请求已改成正常返回空会话，避免入口页显示无意义的网络错误。

## 复现方法

先完成 `npm ci`、`npm run build`，并确保 3218 端口空闲。仅对隔离 QA 服务运行浏览器脚本：脚本会开始新课堂并提交演示记录。

```bash
node scripts/qa-server.js
```

在另一个终端使用 Playwright CLI：

```bash
npx --yes --package @playwright/cli playwright-cli -s=workshop-check open http://127.0.0.1:3218/teacher --headed
npx --yes --package @playwright/cli playwright-cli -s=workshop-check run-code --filename=test/browser/flow.js
```

QA 服务使用 `output/qa-*` 独立数据目录和明确的测试密码。不要将此服务作为真实课堂服务。日常运行方式见 [README.md](README.md)。

## 尚未验证的范围

未在学校服务器部署，未进行真实课堂人数和持续时长的压力验收，未测试实际无线网络、外部反向代理或真实手机硬件；手机检查使用 Chromium 视口模拟。Docker 和 Nginx 文件为部署示例，未运行验收。学校名单已按用户提供的 6 个名称配置，未另行核验学校名称的规范写法。没有外部模型调用，也不存在模型效果验收。
