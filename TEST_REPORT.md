# 验证记录

[English](TEST_REPORT.en.md) · [返回说明](README.md)

验证日期：2026-09-23，版本 0.4.2。环境：本机 macOS、Node.js 24.14.1、Chromium。测试使用独立数据库和演示身份；未读取或迁移原“同频课堂反馈”项目的课堂数据库。

## 自动化检查

`npm run check` 通过：35 项测试通过，0 项失败，生产构建成功。依赖锁文件中的依赖条目与上一版逐项一致，仅项目自身版本更新。0.3.0 时曾在独立目录验证 `npm ci --ignore-scripts --no-audit --no-fund`；本次未重复安装。首版的 `npm audit --omit=dev` 报告 0 项已知漏洞，本次未重新审计。

覆盖根地址进入教师页面且不创建学生身份、教师权限、同源写入、固定链接稳定性、自动加入复用、旧链接隔离、页面总开关、课前等候、主题切换、跳过发现直接提交设计、每个主题独立解锁分享、暂停与恢复、课堂结束、过期控制请求、学校与字段校验、并发去重、CSV、注销、重启、新课堂隔离、旧数据库迁移和真实 HTTP SSE。

未提交者直接请求分享列表、搜索和分页都被拒绝；已提交一个主题不能读取另一个主题。状态接口只包含自己的作品和汇总数量，教师仍可读取全部记录。

并发用例在单机 Fastify 注入层模拟 150 个共享出口地址的参与端，随老师推进完成 300 次有效提交；分页可完整读取。这不代表目标校网的容量上限。

固定链接在重启后保持一致由接口测试验证；旧版含 `code` 参数的链接保留原课堂对应关系。未知或缺失课堂链接被拒绝，页面、分享和提交请求绑定正在访问的课堂，避免浏览器多标签页串课。

删除验证：主题、学校、关键词组合筛选；最多 50 份批量操作；学生与未登录访问拒绝；跨课堂、失效 ID、重复 ID、过量选择及跨站写入被拒绝，错误时整批不改变。删除后正文在数据库中清空为 `{}`，师生列表、搜索、统计和 CSV 均不包含它；作者只看到移除标记，重复提交和恢复请求均不能找回原文。重启后结果仍保持。52 份记录验证删除末页后分页回到有效页，真实 HTTP SSE 验证删除通知。原始正文已不在当前提交数据中，后续词云可使用清理后的列表或 CSV；本轮未实现词云。

## 连接故障与恢复检查

真实 HTTP 检查验证教师 SSE 鉴权、立即发送的握手、流式／禁缓存／禁压缩响应头、20 秒心跳，以及教师注销时关闭连接；学生与未登录者不能读取教师通道。使用可控时钟验证静默 45 秒后重建、旧连接事件丢弃、心跳维持、恢复时补取状态、退出清理以及浏览器不支持 EventSource 的情况。HTTPS 代理分享使用浏览器实际地址，PUBLIC_URL 优先；来自另一份数据库的课堂 ID 被明确拒绝，不会悄悄进入当前课堂。

0.4.1 发布时，`test/browser/connection.js` 主动挂起教师和学生的事件请求，同时保留普通接口。两端显示定时同步，教师收到学生提交、学生跟随主题切换，均无需刷新。随后模拟状态接口也断开，页面显示连接中断；恢复接口后先回到定时同步，再恢复实时连接。不存在的课堂链接保持隔离，正确固定链接复用原学生身份。脚本通过，无页面脚本异常；主动中止网络请求会产生预期的资源错误日志。0.4.2 未改动连接恢复逻辑，未重复故障浏览器脚本；自动化连接测试与普通完整流程已重新通过，无控制台错误。

本轮尚未收到线上地址和部署方式，未检查实际线上代理和数据卷配置；本地故障模拟不能证明线上故障的具体原因。

## 学校名单与升级

6 所学校按用户提供的原名和顺序配置，详见 README。此前隔离校验确认六所均可提交、旧占位名单被拒绝；本次继续验证服务端名单限制。

迁移用例从旧表结构启动，保留原课堂、身份和已提交作品，验证原两个开关同时开放时进入设计主题。新增删除字段默认为空，旧作品仍正常显示。原本的本地预览也在停服备份后升级并启动，原课堂和演示记录保留；删除测试只操作隔离 QA 数据。

## 浏览器闭环

使用一个教师页面和两个独立学生浏览器上下文，完整执行 `test/browser/flow.js`，验证：

1. 教师复制按钮得到正确的固定链接，禁用 Clipboard API 后的备用复制也通过；学生打开链接即自动进入，不出现课堂码输入或手动加入步骤。访问根路径会进入教师登录页，已登录的教师回到工作台；已有学生身份也不会因此加入新课堂。登录页没有“学生入口”，根地址不创建学生身份或增加课堂人数。页面关闭时学生自动进入等候；打开总开关后进入课前等候；老师开启发现主题后，两端自动显示表单，无需刷新。
2. 未提交的学生只看到锁定提示；另一人提交后，该学生仍看不到任何同学作品。提交者右侧显示论坛式列表。
3. 暂停后未提交表单禁用，所有学生看到暂停提示，已解锁分享继续可读；恢复后可继续填写。
4. 老师切换设计主题，两名学生自动进入第二页，包括未提交发现的学生。该学生可以直接提交设计，学校和姓名草稿保留。
5. 发现与设计分别解锁，两个学生在同一主题提交后彼此可见，新增分享自动出现。
6. 老师返回发现时，未提交草稿恢复；刷新后也保留草稿。先交设计的学生回到发现时沿用身份，但分享仍待本主题提交后解锁。
7. 提交记录刷新后恢复；姓名搜索正确过滤，清除搜索后恢复全部条目。
8. 关闭总开关后学生表单和分享都收起，再开放恢复当前主题；结束课堂后两端显示结束页，教师保留记录。
9. 反复切换和结束后教师链接不变；结束后的新访客直接看到结束页且不创建学生身份。开始新课堂后，旧链接和带有新课堂 Cookie 的旧链接访问均停留在原课堂结束页，新课堂人数不重复增加。
10. 1366 × 768 电脑与 1024 × 768 平板横屏保持左右布局，1920 × 1080 大屏无整页横向溢出。以机房电脑为主要使用场景，不再专门适配手机窄屏。
11. 教师按学校及内容组合筛选，切换条件后清空勾选；确认框展示选中姓名和不可恢复提示，取消后记录保留。单条删除后作者及其他学生页面同步移除正文，批量删除另一主题的两份记录后该主题为空，切回原主题仍保留未删除作品。学生端没有删除操作。
12. 登录、教师工作台、学生等候、活动、提交后和结束页面的可见文字均不包含课堂码；学生仍通过固定链接自动进入。

完整脚本通过，无页面脚本异常或控制台错误。已检查根地址跳转后的教师登录页截图，完整流程继续覆盖教师、学生与桌面／平板布局。本地截图及脚本结果保存在 `output/playwright/`，不进入 Git。本地预览已更新到 0.4.2，根地址进入教师页面，原固定链接、教师密码、学生身份和提交记录校验未变。

## 复现方法

先完成 `npm ci`、`npm run build`，并确保 3219 端口空闲。仅对隔离 QA 服务运行浏览器脚本：脚本会开始新课堂、提交及删除演示记录。3218 保留给日常本地预览。

```bash
node scripts/qa-server.js
```

在另一个终端使用 Playwright CLI：

```bash
npx --yes --package @playwright/cli playwright-cli -s=workshop-check open http://127.0.0.1:3219/teacher --headed
npx --yes --package @playwright/cli playwright-cli -s=workshop-check run-code --filename=test/browser/flow.js
npx --yes --package @playwright/cli playwright-cli -s=workshop-check run-code --filename=test/browser/connection.js
```

QA 服务使用 `output/qa-*` 独立数据目录和明确的测试密码。不要将此服务作为真实课堂服务。日常运行方式见 [README.md](README.md)。

## 尚未验证的范围

未在学校服务器部署，未进行真实课堂人数和持续时长的压力验收，未测试实际无线网络、外部反向代理或实体平板及交互大屏；尺寸检查使用 Chromium 视口模拟。Docker 和 Nginx 文件为部署示例，未运行验收。学校名称未另行核验规范写法。没有外部模型调用，也不存在模型效果验收。
